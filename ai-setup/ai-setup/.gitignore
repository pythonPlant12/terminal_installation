# Python
__pycache__/
*.pyc

# local overrides / secrets
defaults/env.local
**/*.bak

# macOS
.DS_Store

# task database
.dolt
.beads/**/**
!.beads/issues.jsonl
!.beads/interactions.jsonl

# nvm version file (machine-specific)
.nvmrc

# agent orchestration — durable plans tracked, ephemeral state ignored (added automatically)
.sisyphus/*
!.sisyphus/README.md
!.sisyphus/plans/
.sisyphus/plans/*
!.sisyphus/plans/README.md

# GSD planning — lifecycle docs tracked, execution state ignored (added automatically)
.planning/STATE.md
.planning/config.json
.planning/debug/

# pipeline step execution state (machine-local, Practice 1; added automatically)
.state/
